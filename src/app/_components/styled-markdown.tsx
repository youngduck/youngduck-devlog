"use client";

export const Blockquote = (props: any) => {
  return <blockquote className="p-4">{props.children}</blockquote>;
};

export const H1 = (props: any) => {
  return <h1 className="text-red-500">{props.children}</h1>;
};
